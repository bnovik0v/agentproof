# agentproof

`agentproof` provides agent-oriented verification challenges for Python services.

The project is intentionally narrow:

- issue deterministic challenges
- serialize them as JSON
- verify responses with explicit failure reasons
- integrate into APIs, gateways, and internal services

It does not provide provider attestation, hardware-backed identity, or anti-fraud guarantees.

